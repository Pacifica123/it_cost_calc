# Единый формат кандидатных альтернатив

## Назначение

`CandidateConfiguration` — общий формат альтернативы, которую можно сравнивать в AHP, генетическом алгоритме, связке GA + AHP, анализе важности критериев и будущем `DecisionReport`.

Раньше в проекте одновременно существовали несколько близких, но разных представлений:

- AHP-конфигурация с полями `id`, `devices`, `meta`;
- GA `candidate_solution` с `selected_items`, `totals`, `score` и диагностикой;
- альтернатива анализа важности критериев с `id`, `name` и таблицей оценок;
- демо-конфигурации, собранные из runtime-записей CAPEX/OPEX.

Этап 4 не удаляет эти форматы мгновенно. Он вводит поверх них общий контракт и адаптеры, чтобы старые вкладки и экспорт продолжали работать, но новые сценарии могли опираться на один тип альтернативы.

## Контракт `CandidateConfiguration`

| Поле | Смысл |
|---|---|
| `id` | стабильный идентификатор альтернативы |
| `name` | человекочитаемое название |
| `scope` | область анализа: `technical`, `software`, `implementation` или `common` |
| `components` | исходные компоненты альтернативы без потери исходных полей |
| `totals` | агрегированные затраты и итоговые числовые суммы |
| `metrics` | предметные метрики: производительность, надёжность, места, лицензии, оценки критериев |
| `source` | источник альтернативы: `manual`, `demo`, `ga`, `catalog`, `imported` |
| `metadata` | служебная информация для совместимости, диагностики и будущего отчёта |

Ключевое разделение:

- `components` отвечают на вопрос **из чего состоит альтернатива**;
- `totals` отвечают на вопрос **сколько стоит альтернатива**;
- `metrics` отвечают на вопрос **какими свойствами обладает альтернатива**;
- `metadata` объясняет **откуда она появилась и как была получена**.

## Допустимые источники

| Источник | Когда используется |
|---|---|
| `manual` | пользователь вручную собрал текущий набор runtime-компонентов |
| `demo` | альтернатива создана демонстрационным сценарием |
| `ga` | альтернатива найдена генетическим алгоритмом |
| `catalog` | альтернатива построена из внешнего каталога или набора каталожных позиций |
| `imported` | альтернатива импортирована из внешнего файла |

## Адаптеры текущего переходного периода

### AHP → `CandidateConfiguration`

Демо-AHP по-прежнему строит legacy-конфигурации с `devices` и `meta`, но рядом формируется `candidate_configurations`. Это позволяет UI продолжать открывать прежний сценарий, а будущему отчёту уже получать общий формат.

### GA → `CandidateConfiguration`

`GeneticOptimizationService` оставляет `candidate_solutions` для совместимости, но дополнительно формирует `candidate_configurations`. В них:

- `components` берутся из `selected_items`;
- `totals` берутся из итогов GA-кандидата;
- `metrics` сохраняют `ga_score`, raw/directed/normalized scores по критериям;
- `metadata` хранит rank, mask, группировку по категориям, критерии и ограничения.

### GA + AHP → общий пул

`GeneticAhpRankingService` ранжирует тот же пул GA-кандидатов и добавляет `candidate_configurations` в AHP-report и export payload. Благодаря этому GA-score, AHP-score и последующее объяснение победителя относятся к одному набору альтернатив.

### Анализ важности критериев → общий пул

`DecisionDemoDataService` добавляет `candidate_configurations` в `criteria_case`, а `run_importance_pipeline` возвращает этот пул в отчёте. Сам алгоритм отношений важности не меняется: он по-прежнему работает с критериями, оценками и отношениями, но теперь результат можно связать с теми же альтернативами, что AHP и GA.

### Runtime CAPEX/OPEX → компоненты конфигурации

`CandidateConfigurationService.from_runtime_entities()` собирает текущие runtime-записи в компоненты одной конфигурации. На этом этапе это переходный адаптер, а не полноценный редактор альтернатив: он нужен, чтобы связать существующие CAPEX/OPEX-записи с будущим `DecisionReport`.

## Правила совместимости

1. Legacy-форматы не удаляются.
2. `configurations`, `candidate_solutions`, `alternatives` и `scores` остаются рабочими.
3. Новый общий пул добавляется рядом как `candidate_configurations`.
4. AHP-агрегация умеет читать как старое поле `devices`, так и новое поле `components`.
5. Экспорт GA + AHP выводит общий пул на верхнем уровне JSON payload.

## Что становится возможным дальше

После Этапа 4 следующие шаги становятся проще:

- TCO считается на уровне одной альтернативы через `TCOModelService`;
- NPV получает финансовые потоки от TCO выбранной конфигурации или текущих итогов затрат;
- `DecisionReport` сможет собрать в одном месте компоненты, стоимость, метрики, GA/AHP/критериальный результат и ограничения;
- добавление нового способа генерации альтернатив не потребует нового ad-hoc-формата.

## Развитие после этапа 5

После Этапа 5 каждая `CandidateConfiguration`, которую собирает application-адаптер, получает блок `totals.tco`. Он содержит стартовые инвестиции, регулярные расходы, стоимость электроэнергии за период и итоговую стоимость владения. Благодаря этому будущий `DecisionReport` сможет ссылаться на один финансовый источник для GA, AHP, гибридной оценки и NPV.

## Ограничения текущего этапа

- UI ещё может показывать старые поля, потому что полная миграция экранов относится к последующим патчам.
- `CandidateConfiguration` пока не является единственным входным форматом всех алгоритмов: адаптеры сохраняют совместимость.
- `DecisionReport` будет вводиться отдельно на следующем этапе roadmap.
